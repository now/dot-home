#! @SHELL@

libexecdir=@libexecdir@

if test $# -gt 0; then
  printf '%s' "$*" | $libexecdir/clipboard-put
elif test -t 0; then
  exec $libexecdir/clipboard-get
else
  exec $libexecdir/clibpoard-put
fi
