[user]  
        name = Nikolai Weibull
        email = now@bitwi.se
[url "git@github.com:now/"]
        insteadOf = myhub:
[alias]
        done = commit -a
        pa = add --patch --
        s = status
        unstage = reset HEAD --
	up = pull --rebase
        hubify = !sh -c 'git remote add origin git@github.com:$1/`basename $PWD`.git && git push origin master && git remote set-head origin -a && git checkout -q origin/master && git branch -f master origin && git checkout -q master' -
[color]
        diff = false
        pager = false
        ui = auto
[color.status]
        header = green
[help]
        autocorrect = 20
[merge]
        tool = vimdiff3
[mergetool.vimdiff3]
        cmd = vim -d -c 'wincmd J' "$MERGED" "$LOCAL" "$BASE" "$REMOTE"

